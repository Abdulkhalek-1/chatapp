from django.urls import path, include
from . import views


urlpatterns = [
   # path('api/v1/channels/<int:channel_id>/messages', views.MessagesView.as_view(), name='Messages-view'),
   # path('api/v1/channels/<int:channel_id>/messages/<int:message_id>', views.MessagesView.as_view(), name='Messages-view'),
   # path("tests/", views.test)
]
